#include <iostream>
#include <string>
#include "point.hpp"

class Polygone {
  private:
    int color [3];
    // create an empty list
    Polygone();
    // add an element to the list
    void append();
  }
  
    void changecolour(R,G,B);
    // changes the colour
  
    // display the polygone
    void display();
    // hide the polygone
    void hide();
  
    //delete the polygone
    ~Polygone;

    // get the surface
    double surface();
}


